// RETIRED: seeding moved to scripts/setup-db.js (PostgreSQL), run with
// `npm run db:setup`. This stub throws so stale imports fail loudly.
throw new Error(
  'src/data/seed.js is retired — run `npm run db:setup` (scripts/setup-db.js) instead.'
);
